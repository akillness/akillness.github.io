---
layout: compress
swcache: true
---

{% comment %}
  Tier-1 search index: metadata + a short snippet only (no full post body).
  Loaded first when the visitor opens the search box so results appear quickly;
  `search.json` (full text) is fetched afterwards and replaces this dataset.
  Keep the field list and the snippet definition in sync with `search.json`.
{% endcomment %}
{% assign searchable_posts = '' | split: '' %}
{% for post in site.posts %}
  {% unless post.hidden == true or post.robots contains 'noindex' %}
    {% assign searchable_posts = searchable_posts | push: post %}
  {% endunless %}
{% endfor %}
[
  {% for post in searchable_posts %}
  {
    "title": {{ post.title | jsonify }},
    "url": {{ post.url | relative_url | jsonify }},
    "categories": {{ post.categories | join: ', ' | jsonify }},
    "tags": {{ post.tags | join: ', ' | jsonify }},
    "date": "{{ post.date }}",
    {% assign _snippet = post.description | default: post.excerpt | strip_html | strip_newlines %}
    "snippet": {{ _snippet | truncate: 200 | jsonify }}
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
